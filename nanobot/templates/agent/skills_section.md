# Skills

The following skills extend your capabilities — reusable, tested workflows for specific tasks (weather, APIs, data pipelines, and so on). Each group lists one absolute root and relative SKILL.md paths; join them when using `read_file`.

**How to use a skill:** when a request matches one of these skills, FIRST `read_file` its `SKILL.md` and follow its exact steps and commands — *before* reaching for generic tools like `web_search`/`web_fetch` or improvising. A skill exists precisely so you don't have to guess: it already contains the right commands, endpoints, and output format for that task. Prefer the skill's documented approach over ad-hoc web scraping.

{{ skills_summary }}
